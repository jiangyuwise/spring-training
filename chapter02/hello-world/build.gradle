jar {
    manifest {
        attributes(
                "Created-By": "Jimmy",
                "Specification-Title": "Pro Spring",
                "Main-Class" : "com.codve.prospring.ch02.xml.HelloWorldSpringDI",
                "Class-Path": configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
